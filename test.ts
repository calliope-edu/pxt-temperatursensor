// tests go here; this will not be compiled when this package is used as a library

//basic.forever(() => {
//    serial.writeValue("0", temperatursensor.TemperatureNumber(temperatursensor.pin.P0))
 //   serial.writeLine("0 : " + temperatursensor.TemperatureString(temperatursensor.pin.P0))
 //   basic.pause(1000)
//})
